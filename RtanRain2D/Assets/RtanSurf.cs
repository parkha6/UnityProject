using UnityEngine;

public class RtanSurf : MonoBehaviour
{
    [SerializeField]
    int RtanHP = 0; //오 진짜 된다. 다 바꿔봐야지.

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
